---
title: "Raport z listy 8"
subtitle: "Modele regresji i ich zastosowania"
author: "Tadeusz Jagniewski,   album 277522"
date: "`r Sys.Date()`"
output: 
  pdf_document:
    toc: true
    fig_caption: yes
    fig_width: 5 
    fig_height: 4 
    number_sections: true
header-includes:
   - \usepackage[OT4]{polski}
   - \usepackage[utf8]{inputenc}
   - \usepackage{graphicx}
   - \usepackage{float}
fontsize: 12pt 
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(warning = FALSE, message = FALSE) 
knitr::opts_chunk$set(fig.pos = "H", out.extra = "", fig.align = "center", dev = "cairo_pdf")
set.seed(123)
library(xtable)
library(MASS)

```



# Wstęp
Rozważamy model regresji nieliniowej
$$Y=g(\mathbf{x}, \mathbf{\beta})+\varepsilon.$$
Niech
$$n=10,\qquad (\beta_1,\beta_2,\beta_3,\sigma)=(10,3,2,1/2)\qquad x_i=\frac{i}{2}\quad\text{dla}\quad i=1,2,\ldots,n.$$







# Zadanie 1
<!-- 
Wygenerować n obserwacji y1, . . . , yn.
-->

Generujemy $n$ obserwacji $y_1,\ldots,y_n$ postaci
$$y_i=\frac{\beta_1 x_i}{\beta_2 + x_i} + \beta_3 + \varepsilon_i,\quad i=1,2\ldots,n,$$
gdzie $\varepsilon_1,\ldots,\varepsilon_n$ są niezależnymi zmiennymi losowymi 
o rozkładzie $\mathcal{N}(0,\sigma^2)$.




```{r}
set.seed(123) 
n <- 10 
x <- (1:n) / 2 
beta.1 <- 10
beta.2 <- 3
beta.3 <- 2
sigma <- 1/2
sigma.kwadrat <- sigma^2
varepsilon <- rnorm(n, mean = 0, sd = sigma)
y <- (beta.1 * x) / (beta.2 + x) + beta.3 + varepsilon
```




# Zadanie 2
<!-- 
Podać wzór na funkcję g(x, β) oraz jej gradient.
-->

Funkcja $g$ jest postaci
$$
g(x, \mathbf{\beta}) 
= 
\frac{\beta_1 x}{\beta_2 + x} + \beta_3
$$
Jej gradient $G$ jest postaci
$$
G(\mathbf{x}, \mathbf{\beta}) = \nabla g(\mathbf{x}, \mathbf{\beta}) 
=
\begin{bmatrix} 
\dfrac{\partial g}{\partial \beta_1} \\
\dfrac{\partial g}{\partial \beta_2} \\
\dfrac{\partial g}{\partial \beta_3}
\end{bmatrix}
=
\begin{bmatrix}
\frac{x}{x + \beta_2} \\
\frac{- \beta_1 x}{(\beta_2 + x)^2}  \\
1
\end{bmatrix}.
$$
Implementujemy te wzory w funkcjach g() oraz gradient().

```{r g}
g<-function(x,beta.1,beta.2,beta.3){
  
  return((beta.1 * x)/(beta.2+x) + beta.3)
  
}
```


```{r gradient}
gradient<-function(x,beta.1,beta.2,beta.3){
  
  g.1 <- x / (x+beta.2)
  g.2 <- ((-beta.1)*x) / (beta.2+x)^2
  g.3 <- 1
  
  return(c(g.1,g.2,g.3))
}
```


# Zadanie 3
<!-- 
Za pomocą algorytmu Gaussa–Newtona wyznaczyć estymatory parametrów 
beta1, beta2, beta2, sigma^2.
(a) Przyjąć odpowiedni punkt startowy algorytmu: (8,2,1)
(b) Przyjąć odpowiednie kryterium stopu.
-->


Przyjmujemy następujące oznaczenia:

\begin{itemize}
  \item $X = [x_{i,j}] \in \mathcal{M}(\mathbb{R}^{n \times p})$ – macierz wartości realizacji zmiennych objaśniających,
  
  \item $Y = [y_1, y_2, \ldots, y_n]^T$ – wartości realizacji zmiennej objaśnianej,
  
  \item $\boldsymbol{\beta}^{(0)} \in \mathbb{R}^p$ – początkowa wartość wektora $\boldsymbol{\beta}$,
  
  \item $g(X;\boldsymbol{\beta}) =
  \big[g(x_1;\boldsymbol{\beta}), g(x_2;\boldsymbol{\beta}), \ldots, g(x_n;\boldsymbol{\beta})\big]^T$
  – wartości dla ustalonej funkcji $g$ w oparciu o zadane parametry $\boldsymbol{\beta}$ i wartości realizacji zmiennych objaśniających,
  
  \item $G^{(j)} =
  \left[
    \dfrac{\partial g(x_i;\boldsymbol{\beta})}{\partial \beta_j}
  \right]_{i,j}
  \in \mathcal{M}(\mathbb{R}^{n \times p})$
  – macierz gradientów funkcji $g$,
  
  \item $L(\boldsymbol{\beta};\sigma^2)
  =
  -\dfrac{n}{2}\log(2\pi)
  -\dfrac{n}{2}\log\sigma^2
  -\sum\limits_{i=1}^{n}
  \dfrac{\big(y_i - g(\,\cdot\,;\boldsymbol{\beta})\big)^2}{2\sigma^2}$
  – logarytm funkcji wiarygodności,
  
  \item $S(\boldsymbol{\beta})
  = \|Y - g(x;\boldsymbol{\beta})\|^2$
  – kwadrat normy wektorów $Y$ i $g(x;\boldsymbol{\beta})$.
\end{itemize}

Kolejne kroki algorytmu są następujące:

\begin{itemize}
  \item wybieramy punkt startowy parametrów
  $\boldsymbol{\beta}^{(0)} \in \mathbb{R}^p$
  oraz liczby $\varepsilon > 0$ i $M \in \mathbb{N}_+$,
  
  \item tworzymy ciąg iteracji:
  \[
    \boldsymbol{\beta}^{(j+1)}
    =
    \boldsymbol{\beta}^{(j)}
    +
    \bigl[(G^{(j)})^T G^{(j)}\bigr]^{-1}
    (G^{(j)})^T
    \bigl[Y - g(X;\boldsymbol{\beta}^{(j)})\bigr],
  \]
  
  \item algorytm zatrzymujemy w kroku $m$, jeśli
  \[
    \|\boldsymbol{\beta}^{(m)} - \boldsymbol{\beta}^{(m-1)}\| \le \varepsilon
    \quad \text{lub} \quad m = M,
  \]
  
  gdzie zgodnie z treścią zadania kładziemy
  $$\varepsilon=0.0001, \qquad M=100.$$
  
  \item przyjmujemy wówczas, że
  $\hat{\boldsymbol{\beta}} = \boldsymbol{\beta}^{(m)}$
  oraz
  $\hat{\sigma}^2 = \hat{\sigma}^2(\hat{\boldsymbol{\beta}})$.
\end{itemize}

W przypadku gdy macierz $(G^{(j)})^T G^{(j)}$ jest nieodwracalna,
liczymy uogólnioną macierz odwrotną np. za pomocą funkcji \textit{ginv()} 
z pakietu \texttt{MASS}.

Implementujemy zatem opisany wyżej algorytm.

Rozpoczynamy od wzorów na $G^{(j)}$ w funkcji \textit{G.j()}.
```{r}
M <- 100
epsilon <- 0.0001

G.j<-function(x,beta.1,beta.2,beta.3){
  g.1 <- x / (x + beta.2)
  g.2 <- ((-beta.1) * x) / (beta.2 + x)^2
  g.3 <- rep(1, length(x))
  return(cbind(g.1, g.2, g.3))
  
}
```

Następnie implementujemy wzory na ciag iteracji $\beta^{(j)}$
w funkcji \textit{beta.j()}.
```{r}
beta.j<-function(x,y,beta.1,beta.2,beta.3){
  Y_hat <- g(x, beta.1, beta.2, beta.3)
  residuła <- y - Y_hat
  
  G <- G.j(x, beta.1, beta.2, beta.3)
  
  beta_curr <- c(beta.1, beta.2, beta.3)
  
  delta <- MASS::ginv(t(G) %*% G) %*% t(G) %*% residuła
  
  beta_next <- beta_curr + as.vector(delta)
  return(beta_next)
}
```

Pozostaje już zaimplementować sam algorytm Gaussa-Newtona 
w funkcji \textit{Gauss.Newton()}.

```{r}
Gauss.Newton<-function(x,y,beta.1,beta.2,beta.3,M,epsilon){

    beta_curr <- c(beta.1, beta.2, beta.3)
  n <- length(y)
  history <- list()
  
  for(m in 1:M){
    Y_hat <- g(x, beta_curr[1], beta_curr[2], beta_curr[3])
    sigma_sq <- sum((y - Y_hat)^2) / n
    history[[m]] <- c(beta_curr, sigma_sq)
    
    beta_next <- beta.j(x, y, beta_curr[1], beta_curr[2], beta_curr[3])
    
    if(sqrt(sum((beta_next - beta_curr)^2)) <= epsilon){
      Y_hat_next <- g(x, beta_next[1], beta_next[2], beta_next[3])
      sigma_sq_next <- sum((y - Y_hat_next)^2) / n
      history[[m + 1]] <- c(beta_next, sigma_sq_next)
      break
    }
    
    beta_curr <- beta_next
  }
  
  wynik <- do.call(rbind, history)
  rownames(wynik) <- NULL
  return(wynik)
}

```

oraz funkcję wiarogodności opisaną powyżej.

```{r}
wiarogodnosc<-function(x,y,beta.1,beta.2,beta.3,sigma){
  n <- length(y)
  Y_hat <- g(x, beta.1, beta.2, beta.3)
  
  L <- -(n/2) * log(2 * pi) - (n/2) * log(sigma) - sum((y - Y_hat)^2) / (2 * sigma)
  return(L)
}
```


# Zadanie 4
<!-- 
Zilustrować działanie algorytmu tabelą przedstawiającą wartości estymatorów 
oraz odpowiadające im wartości logarytmu funkcji
wiarygodności dla kroków s = 0, 1, 2 oraz s = ostatni.
-->



```{r, echo=FALSE}
tab.1<-Gauss.Newton(x,y,
                    beta.1 = 8,
                    beta.2 = 2,
                    beta.3 = 1,
                    M = M,
                    epsilon = epsilon)

w <- numeric(nrow(tab.1))

for (i in 1:nrow(tab.1)) {
  w[i] <- wiarogodnosc(x,
                       y,
                       tab.1[i,1],
                       tab.1[i,2],
                       tab.1[i,3],
                       tab.1[i,4])
}

m1 <- nrow(tab.1)

tab.1.df <- data.frame(kroki = 0:(m1-1),
                       tab.1,
                       w)

wybrane_wiersze <- c(1, 2, 3, m1)
tab.1.df.skrocona <- tab.1.df[wybrane_wiersze, ]

tab.1.df.skrocona[nrow(tab.1.df.skrocona),1] <- paste(tab.1.df.skrocona[nrow(tab.1.df.skrocona),1],"(ostatni)")

colnames(tab.1.df.skrocona) <- c("kroki algorytmu",
                        "$\\hat{\\beta_1}^{(s)}$",
                        "$\\hat{\\beta_2}^{(s)}$",
                        "$\\hat{\\beta_3}^{(s)}$",
                        "$\\hat{\\sigma^2}^{(s)}$",
                        "$L\\left(\\hat{\\beta}^{(s)},\\hat{\\sigma^2}^{(s)}\\right)$")



```


```{r, results="asis", message=FALSE, echo=FALSE}
print(xtable(tab.1.df.skrocona, digits = c(1,1,5,5,5,7,7)),
      sanitize.text.function=function(x){x},
      comment = FALSE,
      include.rownames=FALSE)
```

Estymator Beta 1 zbiega do swojej rzeczywistej wartości, jaką jest 10, podczas gdy estymatory beta 2 i 3 znacząco odbiegają od swoich prawdziwych wartości (obydwa o różnią się od prawdziwego o ponad 2.)


# Zadanie 5

```{r}
beta_prawdziwe <- c(10, 3, 2)
beta_est <- tab.1[nrow(tab.1), 1:3]

porownanie <- rbind("Prawdziwe parametry" = beta_prawdziwe, 
                    "Estymatory z GN" = beta_est)

colnames(porownanie) <- c("$\\beta_1$", "$\\beta_2$", "$\\beta_3$")

knitr::kable(porownanie, 
             digits = 5, 
             caption = "Porównanie prawdziwych parametrów z estymatorami",
             escape = FALSE)
```


Estymator Beta 1 zbiega do swojej rzeczywistej wartości, jaką jest 10, podczas gdy estymatory beta 2 i 3 znacząco odbiegają od swoich prawdziwych wartości (obydwa o różnią się od prawdziwego o ponad 2.)








# Zadanie 6
<!-- 
Na jednym rysunku przedstawić wykresy 

{(x_i, y_i) : i = 1, . . . , n} 

oraz

{(x_i, hat(y_i) : i = 1, . . . , n}.
-->



```{r wykresy, fig.cap="Wykres porównujący obserwacje do dopasowanego modelu"}

y_hat <- g(x, beta_est[1], beta_est[2], beta_est[3])

plot(x, y, pch = 16, col = "black", 
     main = "Wartości oraz wartości dopasowane przez model",
     xlab = "x", ylab = "y")
points(x, y_hat, pch = 4, col = "red", cex = 1.2)
lines(x, y_hat, col = "red", lwd = 2)

legend("bottomright", legend = c("Obserwacje", "Model dopasowany"), 
       col = c("black", "red"), pch = c(16, 4), lty = c(NA, 1))

```

Model stosunkowo dobrze dopasował się do wartości, zwłaszcza biorąc pod uwagę, że wartości do których model miał się dopasować nie mają prostej zależności liniowej.






# Zadanie 7
<!-- 
Oszacować macierz kowariancji estymatora beta.
-->

Wobec wzoru na macierz kowariancji estymatora najmniejszych kwadratów w modelu 
regresji liniowej, przyjmujemy że (macierz $G$ pełni rolę macierzy $X$ z modelu 
regresji)
$$
Cov(\hat{\beta}) = \sigma^2 (G^T G)^{-1}
$$
Aby otrzymać estymator $Cov(\hat{\beta})$ zastępujemy nieznany wektor $\beta$ 
(w $G$) wektorem $\hat{\beta}$, a wariancję $\sigma^2$ szacujemy za pomocą błędu
średniokwadratowego
$$
s^2 = \frac{\sum\limits_{i=1}^{n}\left[y_i - g(x_i,\hat{\beta})\right]^2}{n-p}.
$$

```{r}
kowariancja <- function(s.kwadrat, G){
  return(s.kwadrat * MASS::ginv(t(G) %*% G))
}
```


```{r kowariancja dla tab.1}

n_obs <- length(y)
p_parametry <- 3

beta_est <- tab.1[nrow(tab.1), 1:3]
y_hat_final <- g(x, beta_est[1], beta_est[2], beta_est[3])

s.kwadrat <- sum((y - y_hat_final)^2) / (n_obs - p_parametry)
G_est <- G.j(x, beta_est[1], beta_est[2], beta_est[3])

Cov_beta <- kowariancja(s.kwadrat, G_est)

nazwy_beta <- c("$\\beta_1$", "$\\beta_2$", "$\\beta_3$")
rownames(Cov_beta) <- nazwy_beta
colnames(Cov_beta) <- nazwy_beta

knitr::kable(Cov_beta, 
             digits = 5, 
             caption = "Oszacowana macierz kowariancji estymatora $\\hat{\\beta}$",
             escape = FALSE) 
```


Najmniejszą wariancję wykazuje beta2, z kolei beta3 ma bardzo wysoką. Można też zauważyć, że wartości kowariancji w żadnym przypadku nie są bliskie zeru, więc praktycznie każdy współczynnik jest skorelowany z pozostałą parą. Na wykresie wydawało się, że model jest dobry jednak ta tabela wykazuje jego niestabilność i zaraz w kolejnych zadaniach postaram się to pokazać.


# Zadanie 8
<!-- 
Za pomocą algorytmu Gaussa–Newtona wyznaczyć estymatory parametrów 
beta_1, beta_2, beta_3, sigma^2 dla dwóch innych punktów startowych:
(9.7, 2.9, 2) oraz (1,1,1).
-->

Do samodzielnego zrobienia.

```{r tab.2 oraz tab.3}

tab.2 <- Gauss.Newton(x, y, 9.7, 2.9, 2, M, epsilon)
tab.3 <- Gauss.Newton(x, y, 1, 1, 1, M, epsilon)


```







# Zadanie 9
<!-- 
Porównać wartości estymatorów beta_1, beta_2, beta_3, sigma^2 otrzymane 
dla trzech punktów startowych.
-->


Do samodzielnego zrobienia.



```{r}
zestawienie <- rbind(
  "Start: (8, 2, 1)"     = tab.1[nrow(tab.1), ],
  "Start: (9.7, 2.9, 2)" = tab.2[nrow(tab.2), ],
  "Start: (1, 1, 1)"     = tab.3[nrow(tab.3), ]
)

colnames(zestawienie) <- c("$\\beta_1$", "$\\beta_2$", "$\\beta_3$", "$\\sigma^2$")

knitr::kable(zestawienie, 
             digits = 5, 
             caption = "Porównanie estymatorów dla różnych punktów startowych",
             escape = FALSE) 
```


Po zmianie punktu startowego, w pierwszym przypadku nic się nie zmienia, natomiast w drugim podaje zupełnie inne oszacowania, natomiast są one komplenie inne i znacząco różnią się od tych prawdziwych, więc algorytm G-N w tym przypadku nie spełnia dobrze zadania.






